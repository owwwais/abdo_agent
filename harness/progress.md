# سجل التقدم

## 2026-09-22 — الجلسة 1: التأسيس وM0 وM1

- أُنشئ المستودع (git، uv، Python 3.12) وملفات الـHarness والتوثيق.
- قاعدة PostgreSQL 16 محلية دون Docker عبر ثنائيات pgserver؛ نسخها إلى مسار ASCII لأن مسار المشروع عربي.
- M0: الإعدادات بحواجز البيئات، نماذج وترحيل 0001 (workspaces، الأعضاء، الجلسات، التدقيق، الطابور، نبض العامل)، Supabase Auth عبر JWKS، جلسات خادم وCSRF، fixture دخول محلي مقيد.
- M1: ترحيل 0002، المنتجات والفئات، المصادر بموصلات manual/html/rss/fake_search/غير مهيأة، جالب HTTP آمن (SSRF)، فحص العينة في العامل، التفعيل بتأكيد المالك، الاستيراد اليدوي وCSV، التطبيع وإزالة التكرار المحافظة، سجل منع التواصل.
- واجهة عربية RTL: اليوم، المنتجات، الفئات، المصادر، الشركات، الاستيراد، الإعدادات.
- أخطاء اكتُشفت بالاختبارات وأُصلحت: JSONB كان يحفظ None كـ`null` JSON بدل SQL NULL (بيانات الصفوف لم تكن تُمسح فعليًا)؛ تحميل كسول لـ`updated_at` بعد التحديث في async (`eager_defaults`)؛ صفحة الخطأ بلا سياق القالب.
- النتيجة: 122 اختبارًا ناجحًا، ruff وmypy نظيفان. F-001..F-005 = verified محليًا.
